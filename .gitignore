# The archive and the feature caches are large and fully derivable. Everything
# needed to rebuild the reports without them is tracked.
data/raw/
data/processed/
# The trailing /* matters. Excluding the directory itself means git never looks inside
# it, so the exception below can never apply: a file cannot be re-included once a
# parent directory is excluded. That is what left the shipped fold untracked while it
# sat on every developer's disk, and it took a CI failure to notice.
data/metadata/report/**/checkpoints/*
# One exception: the fold `python -m src.predict` ships with. Without it a fresh clone
# can score the committed predictions but cannot identify a recording. The trees
# rather than the network, because the network is wrong while above 90% confident on
# one prediction in twelve and the trees on one in a thousand.
!data/metadata/report/base_10k/xgboost/checkpoints/fold0.json

# Sweep candidates are fitted to be discarded. Their results run to hundreds of
# megabytes and the whole point of the directory is that nothing published reads them,
# so the numbers that survive a sweep live in docs/confounds.md rather than here.
data/metadata/report/sweep_*/
data/metadata/report/fold_summary_sweep_*.csv

.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
coverage.xml
.coverage
